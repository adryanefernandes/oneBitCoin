import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  headerPrice: {
    width: "100%",
    height: "auto",
    alignItems: 'center',
  },
  currentPrice: {
    color: "#f4931b",
    fontSize: 32,
    fontWeight: 'bold',
    paddingTop: 30,
  },
  textPrice: {
    color: '#fff',
    fontSize: 16,
    fontWeight: 'bold',
    paddingBottom: 30
  }
})